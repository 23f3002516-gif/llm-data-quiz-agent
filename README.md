# LLM Quiz Solver (Node.js + Playwright)

Automated endpoint for the TDS quiz system.
Accepts POST requests, validates secrets, scrapes JS-rendered quiz pages,
solves data tasks, and submits answers automatically.

## Setup

1. Clone repo
2. Run:
   ```bash
   npm install
